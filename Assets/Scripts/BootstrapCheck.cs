using UnityEngine;

public class BootstrapCheck : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("오늘의 정식 Unity 개발 환경이 정상입니다.");
    }

}
