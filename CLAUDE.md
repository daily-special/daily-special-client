# daily-special-client

「오늘의 정식」(Daily Special) — 모험가 길드의 가성비 구내식당을 운영하는 코지 경영 시뮬 게임. **이 repo는 그 게임의 클라이언트(Unity 도트 2D)다.**

**작업 전에 `docs/`를 읽는다.** 이 파일은 그 요약과 작업 규칙이다.

| 문서 | 언제 읽나 |
|---|---|
| `docs/client-design.md` | 항상. 클라가 무엇을 맡고 무엇을 안 맡는가 |
| `docs/conventions.md` | **코드를 쓰기 전에 항상.** 계층·테스트·데이터 로딩 규약 |
| `docs/data-contract.md` | 데이터를 읽을 때. 세 저장소와의 유일한 접점 (**사본**, 원본은 파이프라인) |

---

## 대회 (NAN 2026)

NHN 게임×AI 해커톤, 채용 연계형. **마감 8/10, 혼자.** 예선 제출물: 게임 빌드·시연 영상·게임 소개서·AI 활용 기술 문서.

**제출물 중 빌드와 영상이 여기서 나온다.** 파이프라인은 끝났고 서버는 아직 없다 — 지금 이 저장소가 임계 경로다.

---

## 저장소 구조 (물리 분리, 3 repo)

```
daily-special-pipeline   Python AI 콘텐츠 생성   (우분투) — 완료
daily-special-client     Unity 도트 2D          (윈도우) ← 여기
daily-special-server     Spring Boot 런타임      (우분투) — 클라 다음
```

셋은 **데이터 계약으로만** 만난다 — 파이프라인이 콘텐츠를 JSON으로 뱉고 여기서 소비한다. **코드 공유 없음.**

---

## 게임이 무엇인가

플레이어는 길드 식당의 요리사다. 손님이 요리를 지정하지 않고 **욕구를 흘린다**("속이 좀 상했어. 뜨끈한 걸로"). 무엇을 낼지 고르고, 슬라이더 3축(불 세기·조리 시간·간)으로 어떻게 만들지 맞춘다.

```
요리 선택  →  욕구에 답한다      (유한. 무엇을 낼까)
파라미터   →  취향에 답한다      (연속. 얼마나 잘 만드나)
```

**관계 = 정보 = 실력.** 손님과 친해질수록 취향이 공개되고, 그게 곧 더 잘 만들 수 있는 능력이 된다. 첫 방문엔 욕구 힌트만, 피드백("좀 짠데요")으로 축 하나가 좁혀지고, 단골이 되면 취향 구간이 열린다.

코지 톤이다. 하드 게임오버 없고, 악당도 비극도 없다.

---

## ⭐ 클라의 핵심 책임 — 만족도 엔진 이식

**손님의 판단은 런타임에 LLM을 부르지 않는다.** 오프라인에서 LLM이 만든 페르소나를 결정적 함수가 평가한다. 그게 이 게임의 AI 차별점이고, **그 함수를 C#으로 옮기는 것이 클라의 가장 중요한 일**이다.

```
만족 = 욕구 충족도 × 취향 일치도 × 예산 적합 × 식이 계수     (전부 곱셈, 결과 0~1)
```

파이썬판이 **레퍼런스 구현**이고 그쪽 테스트 23건이 **이식 명세**다. 옮길 때 그 케이스를 그대로 통과시킨다.

> 원본: `daily-special-pipeline`의 `src/daily_special/domain/satisfaction.py`와 `tests/test_satisfaction.py`. 이식 시점(3~4일차)에 그 파일을 직접 보고 옮긴다 — 요약본을 여기 만들어두지 않는다. 두 벌이 되면 요약본이 뒤처진다.

---

## 코드 규칙

- 주석·독스트링·에러 메시지는 **한국어**, 식별자는 영어
- **도메인은 `UnityEngine`을 모른다.** 상세는 `docs/conventions.md` 1절
- 설계 결정은 주석이 아니라 **테스트로 고정**
- **저장소 문서는 외부를 참조하지 않는다** — 자체 완결로 쓴다

---

## 작업 방식

**작업 단위마다 아래 5단계를 돈다.**

### 1. 사전 보고
- **무엇을** 만드는가 — 한두 문단
- **어디를** 건드리는가 — 새로 만들 파일·고칠 파일 목록
- **무엇을 안 하는가** — 이번 범위 밖. 이걸 적어야 범위가 조용히 넓어지지 않는다

### 2. 의사결정 필요 사항 체크
- 사용자의 판단이 필요한 것**만** 올린다. 관례적 기본값이 있으면 **권장안을 먼저 제시**하고 근거 한 줄
- 결정할 게 없으면 **"결정 필요 없음"이라고 명시**한다. 형식을 채우려고 없는 질문을 만들지 않는다
- 게임 수치가 미확정이면 여기서 확인한다

### 3. 컨펌 후 작업
- **컨펌 전에 코드를 쓰지 않는다.**
- 파일을 쓰거나 고칠 때마다 **그 행동을 한 줄로 해설한다** — 어떤 파일에 / 무엇을 / 왜
- 작업 중 계획이 틀어지면 계속 진행하지 말고 **그 자리에서 보고**한다

### 4. 사후 보고
- 한 것 / **안 한 것과 그 이유**
- 빌드·테스트 결과. **실패했으면 실패했다고 쓰고 출력을 함께 보인다**
- 주요 코드는 응답에 함께 보인다
- 작업 중 **내가 판단한 것** 중 사용자가 확인해야 할 것

### 5. 문서 반영
- **문서화할 것이 생겼으면 기존 문서를 고치거나, 마땅한 문서가 없으면 새로 만들어 반영한다.** 결정을 대화에만 남기지 않는다
- 반영하기 전에 **어느 문서가 그 내용을 소유하는지** 먼저 본다
- 반영할 게 없으면 "없음"이라고 말한다

### 브랜치와 PR

- 브랜치명 `<type>/<영문-슬러그>` (예: `feat/day-cycle`)
- **작업 착수 전에 브랜치를 판다.** main에서 작업하지 않는다
- 문서 반영까지 끝나면 PR을 올리고 squash 머지
- 작업 계획은 문서가 소유한다. GitHub 이슈로 옮기지 않는다

커밋은 사용자가 한다. 요청받지 않으면 커밋하지 않고, **커밋 메시지는 응답에 적어준다.**

커밋 메시지는 `<type>: <한국어 설명>` 제목 한 줄, **50자 이내**. (예: `feat: 손님 주문 화면 구현`)

게임 수치가 미확정이면 그럴듯한 값을 지어내지 말고 확인한다.

---

## 현재 상태

**Unity 프로젝트 없음.** 이 저장소에는 문서와 목 데이터만 있다.

첫 작업은 `docs/client-design.md`의 1단계다 — **목 JSON을 읽어 한글을 화면에 띄우고 APK를 폰에 올린다.** 게임 로직보다 먼저 한다. 이유는 그 문서에 있다.
